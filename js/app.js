// Applicazione Javascript

const clientAge = prompt('Gentile Cliente, indichi la sua età')
const clientKm = prompt('Gentile Cliente, indichi qui i km che intende percorrere')

totalKm = (clientKm * 0.21)

console.log(totalKm)